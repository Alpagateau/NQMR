{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run melonDS",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/NQMT.nds",
      "preLaunchTask": "Run in melonDS",
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "${env:DEVKITARM}/bin/arm-none-eabi-gdb",
      "setupCommands": []
    }
  ]
}
